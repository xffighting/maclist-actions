# MacList 0.3.0 Native Preview 验收记录

验收日期：2026-07-15

索引事实基线：`e9b3a49`（完整原生索引流程进入 verify / CI 门禁）

验收范围：静默测试、构建与静态安全检查

结论：**用户授权的本地索引、Spotlight 补充搜索、文件窗口监听，以及失败关闭的“选中文件后自动确认原 Open-file 面板”均已通过本机统一静默门禁；Release 构建、签名、doctor、安装成功与故障回滚也已通过。真实标准面板、微信、Apple Mail、Outlook 兼容性尚未可视化验收，不能据此发布正式版。**

## 验收口径

### 首次设置

1. MacList 私有本地索引默认关闭，不在用户未选择文件夹时扫描。
2. 用户从菜单栏点击 **选择索引文件夹…**，只选择具体客户/项目资料文件夹。
3. MacList 只保存文件名、完整路径、修改时间，以及所选文件夹的 macOS 授权书签和授权版本；不读取正文。
4. 菜单明确显示未设置、正在建立、已就绪、部分完成、需要更新、需要重新选择或失败。
5. 用户可以 **立即更新本地索引**，也可以 **清空本地索引…**；清空不删除原文件。

### 日常流程

1. 用户在微信或邮件中点击“上传文件 / 添加附件”。
2. 系统文件窗口出现，MacList 自动贴附，不打开 Finder，不切到独立应用。
3. 用户输入客户名、项目名、文件名或路径片段，候选清单显示文件名与父目录。
4. 本地索引是主来源，Spotlight 是补充；同一路径以本地索引记录为准。
5. 用户按回车后，MacList 在原窗口中精确选中经过规范化 URL / 路径核对的文件。
6. 默认模式继续确认原单文件 Open 面板，让附件进入当前聊天或邮件的待发送区；MacList 绝不自动发送。安全条件不满足时，保留原窗口并回退人工确认。

菜单中的 **选中文件后自动确认原窗口** 默认开启；用户关闭后即为“只选中、手动确认”模式。

## 本阶段已经落地

- 主流程不再依赖快捷键或居中启动页：文件选择窗口出现后自动贴附搜索栏。
- 系统级焦点发现分别保存宿主应用、文件面板拥有者和运行时键盘目标，避免把微信/邮件 PID 错当成文件面板 PID。
- 实际文件面板 PID 拥有独立 `AXObserver`；深层 AX 分类在专用队列运行。
- 本地索引使用显式文件夹选择、持久化 macOS 书签、授权版本和私有原子存储。
- 宽根目录、符号链接逃逸和不可信目录被拒绝；建立前后核对根目录物理身份。
- 扫描有文件数、访问项数和时间上限；子目录枚举或元数据访问错误也会降低为“部分完成”。安全取得的未完整结果仍可搜索。
- 本地索引结果与 Spotlight 结果按规范化完整路径合并，本地索引优先。
- 查询和索引刷新使用独立 epoch；索引建立、替换、取消和清空使用 generation，旧任务不能恢复旧缓存或覆盖当前候选。
- 回填事务重新验证原文件面板和精确选中；默认模式为 `selectAndConfirm`，同时保留菜单 `selectOnly`。最终确认使用一次性会话凭证，只允许原窗口的权威默认按钮，并且任何结果都不会重试。
- 自动确认必须拒绝 Save、选文件夹、未知类型、多选窗口、旧会话、无法核对的精确选中，以及缺失或含义不唯一的默认按钮；所有拒绝都回退人工，不关闭原窗口。

## 静默门禁覆盖

`./app/Scripts/verify.sh` 是当前统一入口，包含全部 framework-free smoke、可用时的 XCTest、release 构建、plist/签名校验和命令行 doctor，不启动图形界面。

本阶段门禁覆盖：

- 默认关闭：无授权时 provider 为空，不隐式扫描；
- 授权版本与规范化根目录集合不匹配时拒绝旧缓存；
- 损坏书签、不安全存储、无效目录和宽根目录失败关闭；
- 根目录在建立期间被替换时，不持久化、不发布结果；
- 替换文件夹、取消、清空后的旧 generation 不得复活；
- 文件数上限、访问上限、不可访问根目录、子目录枚举和元数据访问错误只产生“部分完成”；
- 索引文件不包含合成样本文档正文；
- 本地索引 + Spotlight 合并、同路径本地优先、来源独立清空；
- 上一条查询和刷新前结果不得覆盖当前搜索；
- 对话框识别、双 PID、焦点保留、会话代次和多屏布局；
- 路径、目录拒绝、规范化选中路径和 `selectOnly` 桥接计划；
- 禁止日常主流程中的 Finder 打开、应用切换、剪贴板注入、AppleScript、旧快捷键和自动发送。

以下新增门禁已由本机统一入口通过；GitHub 完整 XCTest 仍以推送后 Actions 为准：

- `selectAndConfirm` 只允许当前、精确核对、单文件 Open 面板；
- Save / folder / unknown / multi-select / stale session / ambiguous button 全部拒绝自动确认并回退人工；
- 默认偏好、持久化与菜单切换在重启后保持一致；
- 自动确认不包含微信或邮件的“发送”动作。

所有索引测试只使用临时沙盒和合成文件，不扫描用户真实资料目录。

## 明确没有做

- 没有启动 MacList 图形界面或 DialogHarness。
- 没有打开或操作微信、Apple Mail、Outlook。
- 没有发键盘事件、抢焦点、切换 Space 或改变用户当前窗口。
- 没有声称微信、Apple Mail 或 Outlook 已兼容。
- 没有运行自动确认原文件面板的可见测试；这里只把本机静默门禁写成已通过，不把真实兼容性写成已通过。
- 没有创建新 tag、GitHub Release、Developer ID 签名或公证安装包。

## 下一道发布门槛

用户明确允许可视化测试后，按顺序验证：

1. 标准单文件 `NSOpenPanel`：自动贴附、输入焦点、客户/项目/文件/路径候选、精确选中、自动确认、关闭和切换窗口。
2. 安全拒绝矩阵：Save、选文件夹、未知类型、多选、旧会话、未核对选择、缺失/歧义按钮都必须保留原窗口并回退人工。
3. 微信：点击上传后自动出现；回车后自动确认原 Open-file 面板；附件进入待发送区；不自动发送。
4. Apple Mail 与 Outlook：添加附件流程相同，逐个记录 macOS、应用版本、窗口类型和失败阶段。
5. 关闭菜单自动确认后，验证回车只选中文件，用户仍可手动点击“打开”。
6. 连续打开、关闭、切换十次，确认无残留浮层、无旧查询、无旧建立任务、无跨 Space 漂移。
7. 每次推送都继续以 GitHub Actions 对应提交的实际结果为远端门禁；文档和 Dashboard 同步后，才评估 tag、Release、签名与公证。

## 旧原型边界

`scripts/` 的 Cling 动作和 `standalone/` 的 CLI 仍作为早期研究资产保留，但不代表当前原生产品流程，也不能作为微信自动唤起、本地索引或文件窗口回填的验收证据。
